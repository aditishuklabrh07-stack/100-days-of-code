#include <stdio.h>

int main() {
    int a, b;

    
    printf("Enter two NUMBERS: ");
    scanf("%d %d", &a, &b);

    printf("\nBefore swapping: a = %d, b = %d\n", a, b)
    printf("After swapping:  a = %d, b = %d\n", b, a);

    return 0;
}
